[project]
name = "world-model-demos"
version = "0.1.0"
description = "Small, traceable experiments for learning world models (Ha & Schmidhuber, Dreamer, video diffusion)."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "torch>=2.4",
    "gymnasium[classic-control]>=1.0",
    "numpy>=1.26",
    "einops>=0.8",
    "matplotlib>=3.9",
    "tqdm>=4.66",
    "jupyter>=1.1",
]
